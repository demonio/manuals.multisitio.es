<?php
$action_name = View::getVar('action_name');
$list = explode("<$titles>", (new Docs)->read($list));
array_shift($list);
foreach ($list as $block): ?>
    <details>
        <summary><?=explode("</$titles>", $block)[0]?></summary>
        <?=str_replace('<a ', '<a data-ajax="main"', explode("</$titles>", $block)[1])?>
    </details>
<?php endforeach;
